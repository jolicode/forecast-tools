{% extends 'base.html.twig' %}

{% block title %}Delete alert "{{ alert.name }}" | {{ parent() }}{% endblock %}

{% block body %}
    <div>
        <h1>Delete alert "{{ alert.name }}"</h1>

        <p>Are you sure that you want to delete this alert?</p>

        {{ form_start(form) }}
            {{ form_errors(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
